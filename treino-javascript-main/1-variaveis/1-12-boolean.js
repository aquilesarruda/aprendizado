/**
 * Declare duas variáveis const com números e verifique
 * se a primeira é menor que a segunda usando operadores de comparação, imprimindo o resultado.
 */

const a = 8
const b = 13

const valorA = a < b 
const valorB = a === b

console.log(`${a} é o numero e o `, `valor da operaçao:a > b ${valorA}`)
console.log(`${b} é o numero e o `, `valor da operaçao:a = b ${valorB}`)
