/**
 * Escreva um script que declare duas variáveis const com números, some-os e imprima o resultado.
 * Depois, declare duas variáveis const com strings, concatene-as e imprima o resultado.
 */

const num1 = 5
const num2 = 10
const sum = num1 + num2
console.log(`Sum: ${sum}`) // Sum: 15

const str1 = 'Hello'
const str2 = 'World'
const combinedStr = str1 + str2
console.log(`Combined String: ${combinedStr}`) // Combined String: Hello World
