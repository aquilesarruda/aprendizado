/**
 * Declare uma variável const com um número.
 * Declare outra variálel const que recebe o tipo da variável anterior.
 * Imprima ambas as variáveis.
 */

const a = 3
const b = a

console.log(a, b)
